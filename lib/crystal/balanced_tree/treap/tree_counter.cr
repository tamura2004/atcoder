require "crystal/balanced_tree/treap/counter"

module BalancedTree
  module Treap
    class TreeCounter(T)
      getter root : Counter(T)?

      def initialize
        @root = nil_node
      end

      def initialize

      def initialize(@root : Counter(T)?)
    end
  end
end
